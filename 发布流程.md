# 发布流程（发版清单）

> 每次发版照这个走一遍。目标：**源码在 GitHub，成品在网盘，链接在 README。**

## 0. 定版本号
- 改 `gui.py` 里窗口标题的版本号（如 `v0.4.3`）
- 想清楚这一版修了什么、加了什么

## 1. 自检（必须）
```bash
py -3 gui.py --selftest      # 看 _selftest.txt，最后一行必须是 result = OK
```
**打包后还要用 exe 再跑一次 `--selftest`**：只 `import` 成功不代表能用。
（历史教训：zhconv 的字典是运行时才加载的数据文件，没打进去就"一调用就崩"。）

## 2. 打包
```bash
发版.bat                      # 等价于 py -3 fab.py
py -3 fab.py --skip-build      # 只想重算校验值/发行说明，不重新打包
```
产物（都在 `dist\Release\`）：
| 文件 | 用途 |
|:-----|:-----|
| `CathayShelf.exe` | 单文件版成品 |
| `校验值.txt` | SHA256，贴到网盘说明/发行说明 |
| `发行说明_vX.Y.Z.md` | 发行说明骨架，填好网盘链接即可用于 GitHub Release |

## 3. 便携版（可选）
把 `runtime\`（约 92 MB，自带 Python）+ `著录.bat` 一起压成 zip，作为「便携版」上传网盘。

## 4. 上传网盘
- **中国移动云盘**（推荐）：上传 → 生成分享链接
- **百度网盘**（备用，密码 2026）：上传 → 生成分享链接（含提取码）

## 5. 建 GitHub Release
```bash
gh release create vX.Y.Z --title "CathayShelf vX.Y.Z" --notes-file "dist\Release\发行说明_vX.Y.Z.md"
# 需要把成品挂到 Release 上时（可选）：
# gh release upload vX.Y.Z "dist\Release\CathayShelf.exe"
```

## 6. 回填链接
- README「📦 下载」表：把 `<待填：…>` 换成真实网盘链接
- 旧版链接挪到「旧版本」表
- README 尾部「更新日志」补本版条目

---
